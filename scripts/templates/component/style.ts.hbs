import type { VariantProps } from '../../utils/variants'
import { createVariants } from '../../utils/variants'

export const use{{ pascalCase name }}Style = createVariants({
  slots: {
    base: '',
  },
  variants: {},
  compoundVariants: [],
  defaultVariants: {},
})

export type {{ pascalCase name }}StyleSlots = typeof use{{ pascalCase name }}Style['slots']
export type {{ pascalCase name }}StyleProps = VariantProps<typeof use{{ pascalCase name }}Style>
